\documentclass[10pt,twoside]{ltjtarticle}
\usepackage[paperwidth=105mm,paperheight=148mm,margin=1cm]{geometry}
\usepackage{titling}
\usepackage{fancyhdr}
\usepackage{luatexja-ruby}

% Reduce spacing around title
\renewcommand{\maketitlehooka}{
    \vspace{-40pt}
}
\renewcommand{\maketitlehookb}{
    \vspace{-20pt}
}
\renewcommand{\maketitlehookc}{
    \vspace{-40pt}
}

% Spacing on 「 is weird when it starts a paragraph.
% TODO: What is the real way to fix this?
\def\hackyquoteindent{\parindent=4.85pt}

% Set up header with page numbers
\pagestyle{fancy}
\setlength{\headwidth}{85mm}
\fancyhf{} % Clear pre-existing header and footer
\fancyhead[OL]{\thepage}
\fancyhead[ER]{\thepage}
\setlength{\headheight}{30pt} % Make space for the header
\setlength{\headsep}{5pt} % Space between header and text
\renewcommand{\headrulewidth}{0pt} % Hide horizontal line in header


\title{<<TITLE>>}
\author{<<AUTHOR>>}
\date{} % Explicitly set empty date so it doesn't appear.


\begin{document}
\maketitle

<<CONTENT>>

\end{document}
